version: "2"
sql:
- name: postgresql
  schema: "./sql/migrations"
  queries: "./sql/queries"
  engine: "postgresql"
  gen:
    go: 
      sql_package: "pgx/v5"
      out: "internal/db"
      emit_interface: true
  rules:
    - sqlc/db-prepare
    - postgresql-query-too-costly