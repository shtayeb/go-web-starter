version: "2"
sql:
- schema: "./sql/migrations"
  queries: "./sql/queries"
  engine: "postgresql"
  gen:
    go: 
      out: "internal/queries"
      emit_interface: true
  rules:
    - sqlc/db-prepare