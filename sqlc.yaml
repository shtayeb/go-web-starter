version: "2"
sql:
- schema: "./sql/migrations"
  queries: "./sql/queries"
  engine: "postgresql"
  gen:
    go: 
      sql_package: "pgx/v5"
      out: "internal/db"
      emit_interface: true
  rules:
    - sqlc/db-prepare